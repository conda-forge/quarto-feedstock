From c0802196bda8247937466b7a1a2810fee8ae46df Mon Sep 17 00:00:00 2001
From: Michael Sarahan <msarahan@gmail.com>
Date: Thu, 1 Sep 2022 12:05:50 -0500
Subject: [PATCH 1/1] debugging env

---
 package/scripts/common/quarto | 2 +-
 src/core/dart-sass.ts         | 3 +++
 2 files changed, 4 insertions(+), 1 deletion(-)

diff --git a/package/scripts/common/quarto b/package/scripts/common/quarto
index 66959f7..8bf2758 100755
--- a/package/scripts/common/quarto
+++ b/package/scripts/common/quarto
@@ -123,6 +123,6 @@ if [ "$QUARTO_DENO" == "" ]; then
 fi
 
 # Be sure to include any already defined QUARTO_DENO_OPTIONS
-QUARTO_DENO_OPTIONS="--unstable --no-config --cached-only --allow-read --allow-write --allow-run --allow-env --allow-net --allow-ffi ${QUARTO_DENO_OPTIONS}"
+QUARTO_DENO_OPTIONS="--unstable --inspect-brk --no-config --cached-only --allow-read --allow-write --allow-run --allow-env --allow-net --allow-ffi ${QUARTO_DENO_OPTIONS}"
 
 "${QUARTO_DENO}" ${QUARTO_ACTION} ${QUARTO_DENO_OPTIONS} ${QUARTO_DENO_EXTRA_OPTIONS} "${QUARTO_IMPORT_ARGMAP}" "${QUARTO_TARGET}" "$@"
diff --git a/src/core/dart-sass.ts b/src/core/dart-sass.ts
index f8d5295..c4d466d 100644
--- a/src/core/dart-sass.ts
+++ b/src/core/dart-sass.ts
@@ -78,6 +78,9 @@ async function dartCommand(args: string[]) {
     ...args,
   ];
 
+  const env = Deno.env.toObject();
+  console.log("env:", env);
+
   console.log("running " + cmd.join(" "));
 
   // Run the sas compiler
-- 
2.34.1

